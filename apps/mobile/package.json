{
  "name": "@yogapratishthan/mobile",
  "private": true,
  "version": "1.0.0",
  "description": "Yogapratishthan Android app — a native shell onto the live Yogapratishthan admin site.",
  "scripts": {
    "sync": "cap sync android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
